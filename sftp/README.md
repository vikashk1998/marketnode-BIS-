# SFTP Test Directory

This folder mirrors the SFTP server's upload structure. Place bond issuance CSV files here for local testing.

**Path inside the SFTP container:** `/home/bonduser/upload/bonds/`

File naming convention: `BONDS_YYYYMMDD_NNN.csv`

See `PRODUCT.md` Section 5 for file format and validation rules.
See `fixtures/` for sample CSV files.